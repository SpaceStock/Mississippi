name: Master
on:
  push:
    branches:
      - 'master'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Go App
        uses: actions/checkout@v1
        run: docker build -t testsuite:1.0 .